# Known limitations

1. Some Swiss providers use highly dynamic pages; several connectors are scaffolded and marked partial/limited.
2. Buyback quote providers are seeded as configs but not fully automated in this milestone.
3. Web history chart is currently lightweight (table/sparkline-ready JSON).
4. Local tests require dependency installation that may fail in restricted network environments.
